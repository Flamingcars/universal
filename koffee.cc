do local library,themes=loadstring(game:HttpGet("https://raw.githubusercontent.com/i77lhm/Libraries/refs/heads/main/Atlanta/Library.lua"))();library.directory="koffee.cc";local players=game:GetService("Players");local run_service=game:GetService("RunService");local user_input=game:GetService("UserInputService");local gui_service=game:GetService("GuiService");local lighting=game:GetService("Lighting");local teleport_service=game:GetService("TeleportService");local lp=players.LocalPlayer;local ws=workspace;local window;local dim2=UDim2.new;local hex=Color3.fromHex;local rgb=Color3.fromRGB;local rgbseq=ColorSequence.new;local rgbkey=ColorSequenceKeypoint.new;local vec2=Vector2.new;local vec3=Vector3.new;local cfr=CFrame.new;local insert=table.insert;local flags=library.flags;local state={target=nil,drawings={},connections={},highlight=nil,fov_fill=nil,current_sky=nil,shooting=false,spin=0,was_spectating=false,unloaded=false,locks={}};local function connection(signal,fn) local conn=signal:Connect(fn);insert(state.connections,conn);return conn;end local function new_drawing(class,props) local obj=Drawing.new(class);for prop,value in pairs(props) do obj[prop]=value;end insert(state.drawings,obj);return obj;end local function is_knocked(character) local effects=character:FindFirstChild("BodyEffects");if effects then local ko=effects:FindFirstChild("K.O") or effects:FindFirstChild("KO") or effects:FindFirstChild("Knocked") ;if (ko and ko:IsA("BoolValue")) then return ko.Value;end end local humanoid=character:FindFirstChildOfClass("Humanoid");return (humanoid~=nil) and (humanoid.Health<=0) ;end local function is_visible(character,part) if  not flags.wall_check then return true;end local camera=ws.CurrentCamera;if  not camera then return false;end local origin=camera.CFrame.Position;local ray_params=RaycastParams.new();ray_params.FilterType=Enum.RaycastFilterType.Exclude;ray_params.FilterDescendantsInstances={lp.Character,character};local result=ws:Raycast(origin,part.Position-origin ,ray_params);return result==nil ;end local function get_bone(character) if (flags.silent_aim_bone=="Head") then local head=character:FindFirstChild("Head");if head then return head;end end return character:FindFirstChild("HumanoidRootPart");end local function is_friend(player) return library.get_priority(player)=="Friendly" ;end local function is_on_team(player) local my_team=lp.Team;local ok,same=pcall(function() return (my_team~=nil) and (player.Team==my_team) ;end);return (ok and same) or false ;end local function get_lock(player) local lock=state.locks[player];if  not lock then lock={};state.locks[player]=lock;end return lock;end local function clear_lock(player) state.locks[player]=nil;end local function predict_position(origin_pos,target_pos,target_velocity) local speed=flags.prediction_speed or 300 ;local gravity=flags.prediction_drop or 0 ;if (speed<=0) then return nil;end if ((target_velocity.Magnitude<0.1) and (gravity<=0)) then return nil;end local hit_position=target_pos;for _=1,5 do local flight_time=(target_pos-origin_pos).Magnitude/speed ;hit_position=target_pos + (target_velocity * flight_time) ;hit_position=hit_position-vec3(0,0.5 * gravity * flight_time * flight_time ,0) ;end return hit_position;end local function is_eligible(player) if (player==lp) then return nil;end if (flags.ignore_friends and is_friend(player)) then return nil;end if (flags.team_check and is_on_team(player)) then return nil;end local character=player.Character;if  not character then return nil;end local hrp=character:FindFirstChild("HumanoidRootPart");local humanoid=character:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid or (humanoid.Health<=0)) then return nil;end if (flags.forcefield_check and character:FindFirstChildOfClass("ForceField")) then return nil;end if (flags.knocked_check and is_knocked(character)) then return nil;end if (flags.enemy_priority and (library.get_priority(player)~="Enemy")) then return nil;end if flags.distance_check then local my_hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") ;if (my_hrp and ((hrp.Position-my_hrp.Position).Magnitude>(flags.gun_range or 1000))) then return nil;end end if  not (is_visible(character,hrp) or (character:FindFirstChild("Head") and is_visible(character,character.Head))) then return nil;end return character,hrp;end local function screen_distance(camera,world_pos) local pos,on_screen=camera:WorldToViewportPoint(world_pos);if  not on_screen then return math.huge;end local screen_pos=vec2(pos.X,pos.Y + gui_service:GetGuiInset().Y );return (screen_pos-user_input:GetMouseLocation()).Magnitude;end local function get_closest_target() local camera=ws.CurrentCamera;if  not camera then return nil;end local best,best_score=nil,math.huge;local mouse_pos=user_input:GetMouseLocation();local inset=gui_service:GetGuiInset().Y;local my_hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") ;for _,player in pairs(players:GetPlayers()) do local character,hrp=is_eligible(player);if character then local pos,on_screen=camera:WorldToViewportPoint(hrp.Position);if on_screen then local score;if (flags.distance_priority=="Distance") then score=(my_hrp and (hrp.Position-my_hrp.Position).Magnitude) or math.huge ;else score=(vec2(pos.X,pos.Y + inset ) -mouse_pos).Magnitude;end if (flags.fov_check and flags.fov) then if (screen_distance(camera,hrp.Position)>(flags.fov_radius or 100)) then continue;end end if (score<best_score) then best_score=score;best=player;end end end end return best;end local mousemover=mousemover or mousemoverl ;local mouse1press=mouse1press or mouse1_press ;local mouse1release=mouse1release or mouse1_release ;local vinput=nil;pcall(function() vinput=game:GetService("VirtualInputManager");end);local function set_shooting(down) if (state.shooting==down) then return;end state.shooting=down;if (mouse1press and mouse1release) then if down then mouse1press();else mouse1release();end elseif vinput then vinput:SendMouseButtonEvent(0,0,0,down,game,0);end end local tracer_inline=new_drawing("Line",{Visible=false,Thickness=1});local tracer_outline=new_drawing("Line",{Visible=false,Thickness=3});local function update_tracer(camera,target_character) local from_pos,to_pos=nil,nil;if (flags.snap_line and target_character) then local bone=get_bone(target_character);if bone then local pos,on_screen=camera:WorldToViewportPoint(bone.Position);local in_fov=true;if (flags.tracer_fov_check and flags.fov) then in_fov=screen_distance(camera,bone.Position)<=(flags.fov_radius or 100) ;end if (on_screen and in_fov) then local viewport=camera.ViewportSize;from_pos=vec2(viewport.X/2 ,viewport.Y);to_pos=vec2(pos.X,pos.Y);end end end if from_pos then local inline=flags.snap_line_color or {} ;local outline=flags.snap_line_outline_color or {} ;local thickness=flags.target_snap_line_thickness or 1 ;tracer_outline.From=from_pos;tracer_outline.To=to_pos;tracer_outline.Thickness=thickness + 2 ;tracer_outline.Color=outline.Color or Color3.new(0,0,0) ;tracer_outline.Transparency=outline.Transparency or 0 ;tracer_outline.Visible=true;tracer_inline.From=from_pos;tracer_inline.To=to_pos;tracer_inline.Thickness=thickness;tracer_inline.Color=inline.Color or hex("#7D0DC3") ;tracer_inline.Transparency=inline.Transparency or 0 ;tracer_inline.Visible=true;else tracer_inline.Visible=false;tracer_outline.Visible=false;end end local FOV_SEGMENTS=60;local fov_lines,fov_outline_lines={},{};local function hide_ring(pool) for i=1,FOV_SEGMENTS do local line=pool[i];if line then line.Visible=false;end end end local function draw_ring(pool,center,radius,rotation,color1,color2,thickness,transparency) for i=1,FOV_SEGMENTS do local line=pool[i];if  not line then line=new_drawing("Line",{Visible=false,Thickness=1});pool[i]=line;end local a0=math.rad(((i-1) * (360/FOV_SEGMENTS)) + rotation );local a1=math.rad((i * (360/FOV_SEGMENTS)) + rotation );line.From=center + (vec2(math.cos(a0),math.sin(a0)) * radius) ;line.To=center + (vec2(math.cos(a1),math.sin(a1)) * radius) ;line.Color=color1:Lerp(color2,i/FOV_SEGMENTS );line.Thickness=thickness;line.Transparency=transparency;line.Visible=true;end end local function update_fov() if  not flags.fov then hide_ring(fov_lines);hide_ring(fov_outline_lines);if state.fov_fill then state.fov_fill.Visible=false;end return;end local center=user_input:GetMouseLocation();local radius=flags.fov_radius or 100 ;local thickness=math.max(flags.outline_thickness_fov or 1 ,1);local rotation=(flags.custom_rotation_fov or 0) + state.spin ;local c1=flags.fov_1_settings or {} ;local c2=flags.fov_2_settings or {} ;draw_ring(fov_lines,center,radius,rotation,c1.Color or hex("#7D0DC3") ,c2.Color or hex("#7D0DC3") ,thickness,c1.Transparency or 0 );if flags.outline_fov then local o1=flags.outline_fov_settings_1 or {} ;local o2=flags.outline_fov_settings_2 or {} ;draw_ring(fov_outline_lines,center,radius + thickness ,rotation,o1.Color or Color3.new(0,0,0) ,o2.Color or Color3.new(0,0,0) ,1,o1.Transparency or 0 );else hide_ring(fov_outline_lines);end if flags.fill_fov then if  not state.fov_fill then state.fov_fill=new_drawing("Circle",{Visible=false,Filled=true,Thickness=1,NumSides=64});end local f=flags.fill_fov_settings or {} ;state.fov_fill.Position=center;state.fov_fill.Radius=radius;state.fov_fill.Color=f.Color or hex("#7D0DC3") ;state.fov_fill.Transparency=math.clamp(1 -(f.Transparency or 0.5) ,0,1);state.fov_fill.Visible=true;elseif state.fov_fill then state.fov_fill.Visible=false;end end local esp_pool={};local function get_esp_pool(player) local pool=esp_pool[player];if  not pool then pool={box=new_drawing("Square",{Visible=false,Thickness=1,Filled=false}),corners={},name=new_drawing("Text",{Visible=false,Size=13,Center=true,Outline=true,Font=2}),distance=new_drawing("Text",{Visible=false,Size=12,Center=true,Outline=true,Font=2}),weapon=new_drawing("Text",{Visible=false,Size=12,Center=true,Outline=true,Font=2}),health_outline=new_drawing("Line",{Visible=false,Thickness=4,Color=Color3.new(0,0,0)}),health_fill=new_drawing("Line",{Visible=false,Thickness=2})};for i=1,8 do pool.corners[i]=new_drawing("Line",{Visible=false,Thickness=1});end esp_pool[player]=pool;end return pool;end local function hide_esp(pool) pool.box.Visible=false;for i=1,8 do pool.corners[i].Visible=false;end pool.name.Visible=false;pool.distance.Visible=false;pool.weapon.Visible=false;pool.health_outline.Visible=false;pool.health_fill.Visible=false;end local function hide_corner_box(pool) for i=1,8 do pool.corners[i].Visible=false;end end local function draw_corner_box(pool,top_left,width,height,color) local cl=math.max(width/4 ,2);local x,y=top_left.X,top_left.Y;local positions={{x,y,x + cl ,y},{x,y,x,y + cl },{(x + width) -cl ,y,x + width ,y},{x + width ,y,x + width ,y + cl },{x,(y + height) -cl ,x,y + height },{x,y + height ,x + cl ,y + height },{(x + width) -cl ,y + height ,x + width ,y + height },{x + width ,(y + height) -cl ,x + width ,y + height }};for i=1,8 do local line=pool.corners[i];local p=positions[i];line.From=vec2(p[1],p[2]);line.To=vec2(p[3],p[4]);line.Color=color;line.Visible=true;end end local function get_screen_bounds(camera,character) local hrp=character:FindFirstChild("HumanoidRootPart");if  not hrp then return nil;end local min_x,min_y=math.huge,math.huge;local max_x,max_y= -math.huge, -math.huge;local any_in_front=false;for _,part in pairs(character:GetDescendants()) do if part:IsA("BasePart") then local holder=part.Parent;if (holder and (holder:IsA("Accessory") or holder:IsA("Tool"))) then continue;end if ((part~=hrp) and (part.Transparency>=1)) then continue;end if ((part.Position-hrp.Position).Magnitude>6) then continue;end local half=part.Size/2 ;for sx= -1,1,2 do for sy= -1,1,2 do for sz= -1,1,2 do local corner=(part.CFrame * cfr(sx * half.X ,sy * half.Y ,sz * half.Z )).Position;local pos=camera:WorldToViewportPoint(corner);if (pos.Z>0) then any_in_front=true;local x,y=pos.X,pos.Y;if (x<min_x) then min_x=x;end if (x>max_x) then max_x=x;end if (y<min_y) then min_y=y;end if (y>max_y) then max_y=y;end end end end end end end if  not any_in_front then return nil;end return min_x,min_y,max_x-min_x ,max_y-min_y ;end local function update_esp(camera) for _,player in pairs(players:GetPlayers()) do if (player~=lp) then local pool=get_esp_pool(player);local character=player.Character;local hrp=character and character:FindFirstChild("HumanoidRootPart") ;local humanoid=character and character:FindFirstChildOfClass("Humanoid") ;if  not (flags.Enabled and hrp and humanoid and (humanoid.Health>0)) then hide_esp(pool);else local min_x,min_y,width,height=get_screen_bounds(camera,character);if  not (min_x and (width>4) and (height>4)) then hide_esp(pool);else local center=vec2(min_x + (width/2) ,min_y + (height/2) );local top_left=vec2(min_x,min_y);local bottom_right=vec2(min_x + width ,min_y + height );local box_color=(flags.Box_Color and flags.Box_Color.Color) or Color3.new(1,1,1) ;local text_size=math.clamp(height * 0.22 ,8,flags.esp_text_size or 14 );if flags.Boxes then if (flags.Box_Type=="Corner") then pool.box.Visible=false;draw_corner_box(pool,top_left,width,height,box_color);else hide_corner_box(pool);pool.box.Position=top_left;pool.box.Size=vec2(width,height);pool.box.Color=box_color;pool.box.Visible=true;end else pool.box.Visible=false;hide_corner_box(pool);end if flags.Names then pool.name.Text=player.DisplayName   .. " ("   .. player.Name   .. ")" ;pool.name.Size=text_size;pool.name.Position=vec2(center.X,(top_left.Y-text_size) -2 );pool.name.Color=(flags.Name_Color and flags.Name_Color.Color) or Color3.new(1,1,1) ;pool.name.Visible=true;else pool.name.Visible=false;end if flags.Healthbar then local frac=math.clamp(humanoid.Health/math.max(humanoid.MaxHealth,1) ,0,1);local bar_offset=math.clamp(width * 0.08 ,3,6);local bx=top_left.X-bar_offset ;pool.health_outline.Thickness=math.clamp(bar_offset-1 ,2,5);pool.health_fill.Thickness=math.clamp(bar_offset-3 ,1,3);pool.health_outline.From=vec2(bx,top_left.Y + height );pool.health_outline.To=vec2(bx,top_left.Y);pool.health_outline.Visible=true;pool.health_fill.From=vec2(bx,top_left.Y + height );pool.health_fill.To=vec2(bx,top_left.Y + (height * (1 -frac)) );pool.health_fill.Color=((flags.Health_Low and flags.Health_Low.Color) or Color3.new(1,0,0)):Lerp((flags.Health_High and flags.Health_High.Color) or Color3.new(0,1,0) ,frac);pool.health_fill.Visible=true;else pool.health_outline.Visible=false;pool.health_fill.Visible=false;end if flags.Distance then local my_hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") ;local dist=(my_hrp and math.floor((hrp.Position-my_hrp.Position).Magnitude + 0.5 )) or 0 ;pool.distance.Text="["   .. dist   .. "m]" ;pool.distance.Size=math.max(text_size-1 ,8);pool.distance.Position=vec2(center.X,bottom_right.Y + 2 );pool.distance.Color=(flags.Distance_Color and flags.Distance_Color.Color) or Color3.new(1,1,1) ;pool.distance.Visible=true;else pool.distance.Visible=false;end if flags.Weapon then local tool=character:FindFirstChildOfClass("Tool");pool.weapon.Text=(tool and tool.Name) or "None" ;pool.weapon.Size=math.max(text_size-1 ,8);pool.weapon.Position=vec2(center.X,bottom_right.Y + ((flags.Distance and (text_size + 1)) or 2) );pool.weapon.Color=(flags.Weapon_Color and flags.Weapon_Color.Color) or Color3.new(1,1,1) ;pool.weapon.Visible=true;else pool.weapon.Visible=false;end end end end end end local lighting_backup=nil;local SKY_PRESETS={["Classic Night"]=47344,["Broken Sky"]=47339,Oblivion=47343,["The Utter East"]=47346,["Walls Of Autumn"]=47347,["Alien Red"]=47410,["John Tron"]=47431,["The Great West"]=311557,Winterness=311580,Shiverfrost=311594};local SKYBOX_FACES={"SkyboxBk","SkyboxDn","SkyboxFt","SkyboxLf","SkyboxRt","SkyboxUp"};local FOG_OFF=100000;local function find_original_sky() for _,child in pairs(lighting:GetChildren()) do if child:IsA("Sky") then return child;end end return nil;end local sky_state=nil;local function apply_sky(name) local face_id;if name then if (name:sub(1,7)=="custom:") then face_id=tonumber(name:sub(8));else face_id=SKY_PRESETS[name];end end if sky_state then if sky_state.custom then sky_state.custom:Destroy();elseif (sky_state.original and sky_state.original.Parent) then for _,face in pairs(SKYBOX_FACES) do sky_state.original[face]=sky_state.backup[face];end sky_state.original.CelestialBodiesShown=sky_state.backup.CelestialBodiesShown;end sky_state=nil;end if  not face_id then return;end local url="rbxassetid://"   .. face_id ;local original=find_original_sky();if original then local backup={CelestialBodiesShown=original.CelestialBodiesShown};for _,face in pairs(SKYBOX_FACES) do backup[face]=original[face];original[face]=url;end original.CelestialBodiesShown=false;sky_state={original=original,backup=backup};else local custom=Instance.new("Sky");custom.Name="koffee_sky";custom.CelestialBodiesShown=false;for _,face in pairs(SKYBOX_FACES) do custom[face]=url;end custom.Parent=lighting;sky_state={custom=custom};end end local fog_backup=nil;local function apply_fog(removed) if removed then if  not fog_backup then local atmosphere=lighting:FindFirstChildOfClass("Atmosphere");fog_backup={fog_end=lighting.FogEnd,fog_start=lighting.FogStart,atmosphere=atmosphere,density=(atmosphere and atmosphere.Density) or nil };end lighting.FogEnd=FOG_OFF;lighting.FogStart=0;if (fog_backup.atmosphere and fog_backup.atmosphere.Parent) then fog_backup.atmosphere.Density=0;end elseif fog_backup then lighting.FogEnd=fog_backup.fog_end;lighting.FogStart=fog_backup.fog_start;if (fog_backup.atmosphere and fog_backup.atmosphere.Parent) then fog_backup.atmosphere.Density=fog_backup.density;end fog_backup=nil;end end local shadows_backup=nil;local function apply_shadows(disabled) if disabled then if (shadows_backup==nil) then shadows_backup=lighting.GlobalShadows;end lighting.GlobalShadows=false;elseif (shadows_backup~=nil) then lighting.GlobalShadows=shadows_backup;shadows_backup=nil;end end local time_backup=nil;local exposure_backup=nil;local ambient_backup=nil;local function update_world() local wanted_sky;local custom_id=flags.skybox_id and flags.skybox_id:match("%d+") ;if custom_id then wanted_sky="custom:"   .. custom_id ;elseif (flags.skybox_preset and (flags.skybox_preset~="Off")) then wanted_sky=flags.skybox_preset;end if (wanted_sky~=state.current_sky) then state.current_sky=wanted_sky;apply_sky(wanted_sky);end apply_shadows(flags.no_shadows);apply_fog(flags.remove_fog);if flags.fullbright then if  not lighting_backup then lighting_backup={Brightness=lighting.Brightness,OutdoorAmbient=lighting.OutdoorAmbient,Ambient=lighting.Ambient};end lighting.Brightness=2;lighting.OutdoorAmbient=Color3.fromRGB(128,128,128);lighting.Ambient=Color3.fromRGB(128,128,128);elseif lighting_backup then lighting.Brightness=lighting_backup.Brightness;lighting.OutdoorAmbient=lighting_backup.OutdoorAmbient;lighting.Ambient=lighting_backup.Ambient;lighting_backup=nil;end if flags.custom_time then if (time_backup==nil) then time_backup=lighting.ClockTime;end lighting.ClockTime=flags.time_of_day or 14 ;elseif (time_backup~=nil) then lighting.ClockTime=time_backup;time_backup=nil;end if flags.custom_exposure then if (exposure_backup==nil) then exposure_backup=lighting.ExposureCompensation;end lighting.ExposureCompensation=flags.exposure_amount or 0 ;elseif (exposure_backup~=nil) then lighting.ExposureCompensation=exposure_backup;exposure_backup=nil;end if flags.custom_ambient then if (ambient_backup==nil) then ambient_backup=(lighting_backup and lighting_backup.Ambient) or lighting.Ambient ;end lighting.Ambient=(flags.ambient_color and flags.ambient_color.Color) or Color3.fromRGB(128,128,128) ;elseif (ambient_backup~=nil) then lighting.Ambient=ambient_backup;ambient_backup=nil;end end local function restore_world() if lighting_backup then lighting.Brightness=lighting_backup.Brightness;lighting.OutdoorAmbient=lighting_backup.OutdoorAmbient;lighting_backup=nil;end if (time_backup~=nil) then lighting.ClockTime=time_backup;time_backup=nil;end if (exposure_backup~=nil) then lighting.ExposureCompensation=exposure_backup;exposure_backup=nil;end if (ambient_backup~=nil) then lighting.Ambient=ambient_backup;ambient_backup=nil;end apply_fog(false);apply_shadows(false);apply_sky(nil);end local function unload() if state.unloaded then return;end state.unloaded=true;set_shooting(false);for _,conn in pairs(state.connections) do pcall(function() conn:Disconnect();end);end for _,drawing in pairs(state.drawings) do pcall(function() drawing:Remove();end);end if state.highlight then pcall(function() state.highlight:Destroy();end);end local my_humanoid=lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") ;local camera=ws.CurrentCamera;if (my_humanoid and camera) then camera.CameraSubject=my_humanoid;end for _,conn in pairs(library.connections or {} ) do pcall(function() conn:Disconnect();end);end pcall(function() restore_world();end);pcall(function() window.set_menu_visibility(false);end);for _,gui in pairs(library.guis or {} ) do pcall(function() gui:Destroy();end);end if library.cache then pcall(function() library.cache:Destroy();end);end end local panel_registry={};local original_panel=library.panel;library.panel=function(self,options) local cfg=original_panel(self,options);insert(panel_registry,cfg);return cfg;end;local original_watermark=library.watermark;library.watermark=function(self,options) local cfg=original_watermark(self,options);local original_change_text=cfg.change_text;cfg.change_text=function(input) pcall(function() original_change_text(os.date("koffee.cc - Beta - %b %d %Y - %H:%M:%S"));end);end;pcall(function() cfg.change_text(os.date("koffee.cc - Beta - %b %d %Y - %H:%M:%S"));end);return cfg;end;window=library:window({name=os.date("koffee.cc |  - %b %d %Y"),size=dim2(0,750,0,782)});local Aiming=window:tab({name="Aiming"});local Misc=window:tab({name="Misc"});local Visuals=window:tab({name="Visuals"});local World=window:tab({name="World"});local column=Aiming:column();local selec,lock,silent=column:multi_section({names={"Selection","Lock","Silent Aim"}});selec:toggle({name="Enabled",flag="target_selected",tooltip="Manages selection of the target (both lock and aim assist)"}):keybind({name="Aiming",flag="target_selected_bind"});selec:toggle({name="Auto Select",flag="auto_select",tooltip="Selects targets for you. (Edit the delay slider if you want more fps.)"});selec:toggle({name="Only Select Enemies",flag="enemy_priority",tooltip="Only targets users under the priority enemy (through the playerlist)"});selec:dropdown({name="Origin",flag="distance_priority",items={"Mouse","Distance"},default="Mouse",tooltip="Selects targets based on the origin"});selec:slider({name="Delay",min=0,max=1000,default=40,interval=1,suffix="ms",flag="target_selector_refresh_time",tooltip="Used for optimizing the checks and target selection. Use for lower end pcs."});selec:toggle({name="Wall Check",flag="wall_check"});selec:toggle({name="Knocked Check",flag="knocked_check"});selec:toggle({name="ForceField Check",flag="forcefield_check"});selec:toggle({name="Distance Check",flag="distance_check",tooltip="Checks if they are in the distance of the guns range (set in Misc > Checks)"});lock:toggle({name="Enabled",flag="silent_aim"});lock:toggle({name="Auto Shoot",flag="auto_shoot"});lock:dropdown({name="Aim Bone",flag="silent_aim_bone",items={"Hrp","Head"},default="Head"});lock:toggle({name="Invisible Bullets",flag="invis_bullet",tooltip="Hides beam/trail/particle visuals on your own character"});silent:dropdown({name="Method",flag="silent_aim_method",items={"Camera","Mouse"},default="Camera",tooltip="Camera rotates to the target, Mouse requires an executor with mousemover support"});silent:toggle({name="Team Check",flag="team_check",tooltip="Never targets players on your team"});silent:toggle({name="Ignore Friends",flag="ignore_friends",tooltip="Never targets players marked Friendly in the playerlist"});silent:toggle({name="FOV Check",flag="fov_check",tooltip="Only locks when the target is inside the FOV circle"});silent:toggle({name="Prediction",flag="prediction",tooltip="Leads moving targets and compensates bullet drop"});silent:slider({name="Bullet Speed",min=100,max=3000,default=300,interval=10,suffix="st/s",flag="prediction_speed",tooltip="Speed of your bullets, used by Prediction"});silent:slider({name="Bullet Drop",min=0,max=60,default=0,interval=0.1,suffix="",flag="prediction_drop",tooltip="Gravity applied to your bullets, used by Prediction"});silent:toggle({name="Smoothing",flag="smooth_rotation",tooltip="Softens the camera rotation onto the target"});silent:slider({name="Smoothness",min=1,max=100,default=30,interval=1,suffix="%",flag="smoothness",tooltip="Higher = slower/smoother snap (used by Smoothing)"});silent:toggle({name="Mouse Smoothing",flag="mouse_smooth",tooltip="Moves the cursor a fraction of the distance each frame (Mouse method)"});silent:slider({name="Mouse Divisor",min=1,max=20,default=4,interval=1,flag="mouse_smooth_divisor",tooltip="Higher = slower cursor movement (used by Mouse Smoothing)"});silent:toggle({name="Hit Chance",flag="hit_chance",tooltip="Rolls a chance to auto shoot on each new target"});silent:slider({name="Chance",min=1,max=100,default=80,interval=1,suffix="%",flag="hit_chance_amount",tooltip="Chance to fire on a new target (used by Hit Chance)"});silent:toggle({name="Target Notifications",flag="notify_targets",tooltip="Notifies when a new target gets locked"});local column=Aiming:column();local vis,other=column:multi_section({names={"Visuals","Other"}});other:toggle({name="Look At",flag="look_at"});other:toggle({name="Spectate",flag="spectate"});vis:toggle({name="Tracer",flag="snap_line"}):colorpicker({name="Tracer Inline",flag="snap_line_color",color=hex("#7D0DC3")}):colorpicker({name="Tracer Outline",flag="snap_line_outline_color",color=hex("#000000")});vis:slider({name="Thickness",min=1,max=5,default=1,interval=1,suffix="°",flag="target_snap_line_thickness"});vis:toggle({name="FOV Check",flag="tracer_fov_check",default=true,tooltip="Only draws the tracer when the target is inside the FOV circle"});vis:toggle({name="Highlight",flag="target_highlight"}):colorpicker({name="Outline",flag="target_highlight_outline",color=hex("#000000")}):colorpicker({name="Fill",flag="target_highlight_fill",color=hex("#000000")});vis:toggle({name="Field Of View",flag="fov"}):colorpicker({name="1st Color (Gradient)",flag="fov_1_settings",color=hex("#7D0DC3"),alpha=0.5}):colorpicker({name="2nd Color (Gradient)",flag="fov_2_settings",color=hex("#7D0DC3"),alpha=0.5});vis:toggle({name="Outline",flag="outline_fov"}):colorpicker({name="1st Color (Gradient)",flag="outline_fov_settings_1",color=hex("#000000"),alpha=1}):colorpicker({name="2nd Color (Gradient)",flag="outline_fov_settings_2",color=hex("#000000"),alpha=1});vis:toggle({name="Fill",flag="fill_fov"}):colorpicker({name="Fill Color",flag="fill_fov_settings",color=hex("#7D0DC3"),alpha=0.5});vis:slider({name="Radius",min=0,max=1000,default=100,interval=1,flag="fov_radius"});vis:slider({name="Thickness",min=0,max=5,default=1,interval=1,flag="outline_thickness_fov"});vis:slider({name="Custom Rotation",min= -180,max=180,default=0,interval=1,flag="custom_rotation_fov"});vis:toggle({name="Spin",flag="spin_fov"});vis:slider({name="Rotation Speed",min=0,max=100,default=100,interval=1,flag="spin_speed_fov"});library.config_flags['fov'](false);local world_column=World:column();local world_lighting=world_column:section({name="Lighting",toggle=false});world_lighting:toggle({name="Fullbright",flag="fullbright",tooltip="Removes darkness for maximum visibility"});world_lighting:toggle({name="No Shadows",flag="no_shadows",tooltip="Disables global shadows"});world_lighting:toggle({name="Remove Fog",flag="remove_fog",tooltip="Removes fog and atmosphere haze for long distance visibility"});world_lighting:toggle({name="Custom Time",flag="custom_time",tooltip="Forces a time of day regardless of the game's day cycle"});world_lighting:slider({name="Time Of Day",min=0,max=24,default=14,interval=0.1,suffix="h",flag="time_of_day",tooltip="Used by Custom Time (0 = midnight, 12 = noon, 24 = midnight)"});world_lighting:toggle({name="Custom Exposure",flag="custom_exposure",tooltip="Overrides the game's exposure compensation"});world_lighting:slider({name="Exposure",min= -3,max=3,default=0,interval=0.1,flag="exposure_amount",tooltip="Used by Custom Exposure (negative = darker, positive = brighter)"});world_lighting:toggle({name="Custom Ambient",flag="custom_ambient",tooltip="Tints all shadowed surfaces with a custom color"});world_lighting:colorpicker({name="Ambient Color",flag="ambient_color",color=hex("#7D0DC3"),alpha=1});local world_column2=World:column();local world_sky=world_column2:section({name="Sky",toggle=false});world_sky:dropdown({name="Skybox",flag="skybox_preset",items={"Off","Classic Night","Broken Sky","Oblivion","The Utter East","Walls Of Autumn","Alien Red","John Tron","The Great West","Winterness","Shiverfrost"},default="Off",scrolling=true});world_sky:textbox({placeholder="Custom Skybox ID...",flag="skybox_id"});local misc_column=Misc:column();local checks=misc_column:section({name="Checks",toggle=false});checks:slider({name="Gun Range",min=50,max=5000,default=1000,interval=10,suffix="st",flag="gun_range",tooltip="Maximum distance used by the Distance Check option in Aiming"});local misc_column=Misc:column();local menu=misc_column:section({name="Menu",toggle=false});menu:button({name="Unload",callback=unload});menu:button({name="Rejoin",callback=function() teleport_service:Teleport(game.PlaceId,lp);end});local esp;local function update_elements() if (esp and esp.refresh_elements) then esp.refresh_elements();end end local column=Visuals:column();local section=column:section({name="General",toggle=false});section:toggle({name="Enabled",flag="Enabled",callback=update_elements});section:toggle({name="Names",flag="Names",callback=update_elements}):colorpicker({flag="Name_Color",callback=update_elements});local settings=section:toggle({name="Boxes",flag="Boxes",callback=update_elements});section:dropdown({name="Box Type",flag="Box_Type",items={"Corner","Full"},default="Corner",callback=update_elements});settings:colorpicker({name="Box Color",flag="Box_Color",callback=update_elements});local toggle=section:toggle({name="Healthbar",flag="Healthbar",callback=update_elements});toggle:colorpicker({name="High HP Color",flag="Health_High",callback=update_elements});toggle:colorpicker({name="Low HP Color",flag="Health_Low",callback=update_elements});section:toggle({name="Distance",flag="Distance",callback=update_elements}):colorpicker({name="Distance Color",flag="Distance_Color",callback=update_elements});section:toggle({name="Weapon",flag="Weapon",callback=update_elements}):colorpicker({name="Weapon Color",flag="Weapon_Color",callback=update_elements});section:slider({name="Text Size",min=10,max=22,default=14,interval=1,flag="esp_text_size",callback=update_elements,tooltip="Maximum ESP text size (text scales down automatically with distance)"});esp=window.esp_section:esp_preview({});Aiming.open_tab();library:update_theme("accent",hex("#7D0DC3"));library:update_theme("glow",hex("#7D0DC3"));library:update_theme("contrast",rgbseq({rgbkey(0,rgb(41,41,41)),rgbkey(1,rgb(35,35,35))}));if (flags.Accent and flags.Accent.Color) then flags.Accent.Color=hex("#7D0DC3");end library.panel=original_panel;for index,panel in pairs(panel_registry) do if (index>1) then panel.items.sgui.Enabled=false;end end task.spawn(function() while  not state.unloaded do if  not flags.target_selected then state.target=nil;else local bind=flags['target_selected_bind'];local bind_active=(bind~=nil) and (bind.active==true) ;if (flags.auto_select or bind_active) then local picked=get_closest_target();if (picked or flags.auto_select) then state.target=picked;end end local character=state.target and state.target.Character ;local humanoid=character and character:FindFirstChildOfClass("Humanoid") ;if (state.target and ( not humanoid or (humanoid.Health<=0))) then state.target=nil;end end task.wait(math.max((flags.target_selector_refresh_time or 40)/1000 ,0.01));for player in pairs(state.locks) do local parented=player and (player.Parent==players) ;if ( not parented or  not player.Character) then clear_lock(player);end end end end);local last_clock=os.clock();connection(run_service.RenderStepped,function() if state.unloaded then return;end local camera=ws.CurrentCamera;if  not camera then return;end local now=os.clock();local dt=now-last_clock ;last_clock=now;if flags.spin_fov then state.spin=state.spin + (dt * (flags.spin_speed_fov or 100) * 3) ;end local target=state.target;local target_character=target and target.Character ;local locked=false;if (flags.silent_aim and target_character) then local bone=get_bone(target_character);if bone then locked=true;local lock=get_lock(target);lock.time=now;local aim_pos=bone.Position;if flags.prediction then local root_part=target_character:FindFirstChild("HumanoidRootPart");local origin=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") ;if (root_part and origin) then local predicted=predict_position(origin.Position,bone.Position,root_part.AssemblyLinearVelocity);if predicted then aim_pos=predicted;end end end if  not lock.notified then lock.notified=true;if flags.notify_targets then library:notification({text="Locked onto "   .. target.Name ,time=3});end end if (flags.silent_aim_method=="Mouse") then if mousemover then local pos,on_screen=camera:WorldToViewportPoint(aim_pos);if on_screen then local screen_pos=vec2(pos.X,pos.Y + gui_service:GetGuiInset().Y );local mouse_pos=user_input:GetMouseLocation();local delta=vec2(screen_pos.X-mouse_pos.X ,screen_pos.Y-mouse_pos.Y );if flags.mouse_smooth then delta=delta/math.max(flags.mouse_smooth_divisor or 4 ,1) ;end mousemover(delta.X,delta.Y);end end else local new_cframe=CFrame.lookAt(camera.CFrame.Position,aim_pos,camera.CFrame.UpVector);if flags.smooth_rotation then camera.CFrame=camera.CFrame:Lerp(new_cframe,math.clamp(1 -(math.max(flags.smoothness,1)/100) ,0,1));else camera.CFrame=new_cframe;end end end elseif target then clear_lock(target);end if (flags.auto_shoot and locked) then local fire=true;if (flags.hit_chance and state.target) then local lock=get_lock(state.target);local roll=lock and lock.hit_roll ;if (roll==nil) then roll=math.random(1,100)<=(flags.hit_chance_amount or 100) ;lock.hit_roll=roll;end fire=roll;end if fire then set_shooting(true);else set_shooting(false);end else set_shooting(false);end if (flags.look_at and target_character) then local my_hrp=lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") ;local bone=get_bone(target_character);if (my_hrp and bone) then my_hrp.CFrame=CFrame.lookAt(my_hrp.Position,vec3(bone.Position.X,my_hrp.Position.Y,bone.Position.Z));end end local target_humanoid=target_character and target_character:FindFirstChildOfClass("Humanoid") ;if (flags.spectate and target_humanoid) then state.was_spectating=true;if (camera.CameraSubject~=target_humanoid) then camera.CameraSubject=target_humanoid;end elseif state.was_spectating then state.was_spectating=false;local my_humanoid=lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") ;if my_humanoid then camera.CameraSubject=my_humanoid;end end if (flags.target_highlight and target_character) then if  not state.highlight then state.highlight=Instance.new("Highlight");state.highlight.DepthMode=Enum.HighlightDepthMode.AlwaysOnTop;end local outline=flags.target_highlight_outline or {} ;local fill=flags.target_highlight_fill or {} ;state.highlight.OutlineColor=outline.Color or Color3.new(0,0,0) ;state.highlight.FillColor=fill.Color or Color3.new(0,0,0) ;state.highlight.OutlineTransparency=outline.Transparency or 0 ;state.highlight.FillTransparency=fill.Transparency or 0.5 ;state.highlight.Adornee=target_character;state.highlight.Parent=target_character;elseif state.highlight then state.highlight.Adornee=nil;state.highlight.Parent=nil;end update_world();update_tracer(camera,target_character);update_fov();update_esp(camera);end);local function suppress_bullet_visual(inst) if (inst:IsA("Beam") or inst:IsA("Trail") or inst:IsA("ParticleEmitter") or inst:IsA("Fire") or inst:IsA("Smoke")) then inst.Enabled=false;end end local function watch_character(character) for _,inst in pairs(character:GetDescendants()) do if flags.invis_bullet then suppress_bullet_visual(inst);end end connection(character.DescendantAdded,function(inst) if flags.invis_bullet then task.defer(suppress_bullet_visual,inst);end end);end connection(lp.CharacterAdded,function(character) task.wait(0.3);if  not state.unloaded then watch_character(character);end end);if lp.Character then watch_character(lp.Character);end connection(players.PlayerRemoving,function(player) local pool=esp_pool[player];if pool then hide_esp(pool);end clear_lock(player);end);library:config_list_update();for index,value in pairs(themes.preset) do pcall(function() library:update_theme(index,value);end);end task.wait();library.old_config=library:get_config();do local wanted_sky;local custom_id=flags.skybox_id and flags.skybox_id:match("%d+") ;if custom_id then wanted_sky="custom:"   .. custom_id ;elseif (flags.skybox_preset and (flags.skybox_preset~="Off")) then wanted_sky=flags.skybox_preset;end if wanted_sky then state.current_sky=wanted_sky;pcall(apply_sky,wanted_sky);end end end
